package app.persistence.adapter;

import org.springframework.stereotype.Service;


@Service
public class BookServiceImpl {

    /*private final Book bookRepository;

    public BookServiceImpl(Book bookRepository) {
        this.bookRepository = bookRepository;
    }

    public app.persistence.entity.Book addOrUpdateBook(app.persistence.entity.Book book) {
        return bookRepository.save(book);
    }

    public List<app.persistence.entity.Book> searchBooks(String title, String author, String isbn) {
        return bookRepository.findAllByMultipleCriteria(title, author, isbn);
    }*/
}
